#include<stdio.h>

int main()
{
    char name[40],address[50];
    puts("Enter your name:");
    gets(name);
    puts("Enter your address:");
    gets(address);
    puts("Your name is:");
    puts(name);
    puts("Your address is:");
    puts(address);
    return 0;
}
